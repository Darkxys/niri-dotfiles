# Copy to apps.env to override app keybind targets without editing keybinds.kdl.
# apps.env is ignored by git.

# NIRI_APP_EDITOR='code --enable-features=UseOzonePlatform --ozone-platform=wayland --disable-gpu'
# NIRI_APP_BROWSER='google-chrome-stable'
# NIRI_APP_NOTES='obsidian'

